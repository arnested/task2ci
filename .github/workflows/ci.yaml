# THIS FILE IS AUTOGENERATED by task2ci. DO NOT EDIT.
# Source: .task2ci/workflows/ci.yaml
# To change CI: edit the source template (or Taskfile.yaml), then run `task2ci`.
---
name: ci

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Check generated CI is up to date
        run: go tool task2ci -check
      - name: Build the task2ci binary
        run: go tool task build

  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Check generated CI is up to date
        run: go tool task2ci -check
      - name: Run unit tests
        run: go tool task test

      - name: Run go vet
        run: go tool task vet

      - name: Run golangci-lint
        run: go tool task lint

      - name: Lint YAML files
        run: go tool task yamllint

      - name: Lint Markdown files
        run: go tool task markdownlint
