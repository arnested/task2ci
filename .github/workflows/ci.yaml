# THIS FILE IS AUTOGENERATED FROM Taskfile.yaml. DO NOT EDIT.
---
name: ci

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install task2ci
        uses: arnested/setup-task2ci@v1

      - name: Check generated CI is up to date
        run: task2ci -check

      - name: Build the task2ci binary
        run: go tool task build

  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install task2ci
        uses: arnested/setup-task2ci@v1

      - name: Check generated CI is up to date
        run: task2ci -check

      - name: Run unit tests
        run: go tool task test

      - name: Run go vet
        run: go tool task vet

      - name: Run golangci-lint
        run: go tool task lint

      - name: Lint YAML files
        run: go tool task yamllint

      - name: Lint Markdown files
        run: go tool task markdownlint

      - name: Validate task2ci.schema.json
        run: go tool task schema-check
